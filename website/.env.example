# Copy to .env and fill in your values before running scripts/deploy.sh.
# .env is gitignored; this file is the committed template.

# SSH user and host for the deploy target (rsync + nginx reload).
SSH_TARGET=user@example.com

# SSH port (default 22; change if the server listens on a non-standard port).
SSH_PORT=22
