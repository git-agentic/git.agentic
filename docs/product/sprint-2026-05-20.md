# Sprint plan — 2026-05-20 (hardening sprint)

**Window:** 2026-05-20 → 2026-05-25 (one compressed week; previously two weeks ending 2026-06-02).
**Shape:** hardening, not feature-add.
**Ship target updated 2026-05-21:** 2026-05-26 with the repo going public — pulled forward from the originally-planned 2026-08-11.

## Why this sprint looks different

Per the audit of `main` on 2026-05-20 (see (internal planning document, not in this repo)), implementations for weeks 1–11 of the roadmap have all landed. Code is leading the calendar.

The current risk is **not** "can we build it." The risk is **false confidence**: code passes thin tests but has not been exercised against (a) the cold-start broken-prompt demo discipline or (b) the documented end-of-week-8 Executor kill-criterion. This sprint converts "LANDED but unverified" into "LANDED and proven."

## Gaps (ranked)

1. **GCS integration tests are `#[ignore]`d** — kill-criterion for ADR-0003 Decision 2 is open until they run green.
2. **Cold-start demo unproven** — < 5 min from `git clone` is aspirational without a fresh-machine run.
3. **No published performance numbers** for the `snapshot-model.md` §9 targets.
4. **No screencast / asciinema** of the demo.
5. **Executor sidecar packaging missing** (ADR-0004 — week 10 deliverable).
6. **Design-partner pipeline empty** (originally week 12; now post-launch — lead time is still weeks, so B3 below remains in-sprint as outreach, not onboarding).
7. **Python SDK not on test PyPI** (week 10).

## Week A — demo discipline + kill-criterion close-out

- **A1** Cold-start the broken-prompt demo on a fresh machine; fix what breaks; record the timings.
- **A2** Un-`#[ignore]` `crates/agentic-core/tests/gcs_integration.rs`; wire `fake-gcs-server` into CI; do one real-bucket parity run. If tests fail → ADR-0003 escape hatch, immediately.
- **A3** Publish `docs/architecture/benchmarks.md` with measured commit / rollback / diff / p99-write numbers vs `snapshot-model.md` §9.
- **A4** 90-second asciinema of the demo, linked from `examples/langgraph-rollback/README.md`.

## Week B — forward-load post-launch *(compressed; runs alongside Week A given the 2026-05-26 ship)*

- **B1** Sidecar `agenticd` container image + `docs/integration/executor-sidecar.md` per ADR-0004.
- **B2** Python SDK published to TestPyPI as `0.1.0a1`; release job in CI; clean-venv install verified.
- **B3** `docs/product/design-partners.md` — outreach brief + ≥ 8 named candidates.
- **B4** Memo `docs/integration/executor-harness-check.md` confirming (or contradicting) ADR-0004 Decision 3's `on_checkpoint`-style harness assumption.

## Out of scope

Web UI, hosted SaaS, additional memory backends, additional framework integrations, remote/in-process daemon deployments, auth/RBAC. All deferred per CLAUDE.md and the in-repo MVP spec.

## Verification

Each item passes when its concrete check passes (see the full plan in (internal planning document, not in this repo) §Verification). Open one tracking issue per A1–A4 and B1–B4.
